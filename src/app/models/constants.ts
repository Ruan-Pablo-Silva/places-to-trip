export class Constants {
  // Defining a constant object with screen names as properties
  static readonly SCREENS = {
    TRIP_LOCATION: '/choose-trip-locations',
    UPDATE_DATA: '/update-data',
  };
}
